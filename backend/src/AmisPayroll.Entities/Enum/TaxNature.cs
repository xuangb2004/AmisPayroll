namespace AmisPayroll.Entities.Enum
{
    public enum TaxNature
    {
        Taxable = 1,            // Chịu thuế
        FullyTaxExempt = 2,     // Miễn thuế toàn phần
        PartiallyTaxExempt = 3  // Miễn thuế một phần
    }
}