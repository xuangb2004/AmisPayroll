/**
 * DevExtreme (esm/__internal/grids/tree_list/module_not_extended/ai_column.integration.test.js)
 * Version: 25.2.7
 * Build date: Tue May 05 2026
 *
 * Copyright (c) 2012 - 2026 Developer Express Inc. ALL RIGHTS RESERVED
 * Read about DevExtreme licensing here: https://js.devexpress.com/Licensing/
 */
import {
    afterEach,
    beforeEach,
    describe,
    expect,
    it,
    jest
} from "@jest/globals";
import $ from "../../../../core/renderer";
import TreeList from "../../../../ui/tree_list";
import {
    AIIntegration
} from "../../../core/ai_integration/core/ai_integration";
import {
    TreeListModel
} from "../../../grids/tree_list/__tests__/__mock__/model/tree_list";
const SELECTORS = {
    treeListContainer: "#treeListContainer"
};
const TREELIST_CONTAINER_ID = "treeListContainer";
const items = [{
    id: 1,
    parentId: 0,
    name: "Name 1",
    value: 10
}, {
    id: 2,
    parentId: 1,
    name: "Name 2",
    value: 20
}];
const createTreeList = async function() {
    let options = arguments.length > 0 && void 0 !== arguments[0] ? arguments[0] : {};
    return new Promise(resolve => {
        const $container = $("<div>").attr("id", "treeListContainer").appendTo(document.body);
        const instance = new TreeList($container.get(0), options);
        const component = new TreeListModel($container.get(0));
        jest.runAllTimers();
        resolve({
            $container: $container,
            component: component,
            instance: instance
        })
    })
};
const beforeTest = () => {
    jest.useFakeTimers()
};
const afterTest = () => {
    const $container = $(SELECTORS.treeListContainer);
    const treeList = $container.dxTreeList("instance");
    treeList.dispose();
    $container.remove();
    jest.clearAllMocks();
    jest.useRealTimers()
};
describe("AI data", () => {
    beforeEach(beforeTest);
    afterEach(afterTest);
    describe("when prompt is set", () => {
        it("should be rendered", async () => {
            const {
                component: component
            } = await createTreeList({
                dataSource: items,
                keyExpr: "id",
                parentIdExpr: "parentId",
                autoExpandAll: true,
                columns: [{
                    dataField: "id",
                    caption: "ID"
                }, {
                    dataField: "name",
                    caption: "Name"
                }, {
                    dataField: "value",
                    caption: "Value"
                }, {
                    type: "ai",
                    caption: "AI Column",
                    name: "myColumn",
                    ai: {
                        prompt: "Initial prompt",
                        aiIntegration: new AIIntegration({
                            sendRequest: () => ({
                                promise: new Promise(resolve => {
                                    resolve('{"1":"AI Response 1","2":"AI Response 2"}')
                                }),
                                abort: () => {}
                            })
                        })
                    }
                }]
            });
            expect(component.getDataCell(0, 3).getText()).toBe("AI Response 1");
            expect(component.getDataCell(1, 3).getText()).toBe("AI Response 2")
        })
    })
});
