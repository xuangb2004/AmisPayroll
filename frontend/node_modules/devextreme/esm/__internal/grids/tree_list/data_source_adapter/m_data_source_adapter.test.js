/**
 * DevExtreme (esm/__internal/grids/tree_list/data_source_adapter/m_data_source_adapter.test.js)
 * Version: 25.2.7
 * Build date: Tue May 05 2026
 *
 * Copyright (c) 2012 - 2026 Developer Express Inc. ALL RIGHTS RESERVED
 * Read about DevExtreme licensing here: https://js.devexpress.com/Licensing/
 */
import {
    afterEach,
    beforeEach,
    describe,
    expect,
    jest,
    test
} from "@jest/globals";
import {
    Deferred
} from "../../../../core/utils/deferred";
import CustomStore from "../../../../data/custom_store";
import DataSource from "../../../../data/data_source";
import {
    DataSourceAdapterTreeList
} from "./m_data_source_adapter";
describe("TreeList DataSourceAdapter - T1311885 Race Condition", () => {
    let dataSourceAdapter;
    let mockStore;
    let loadCalls;
    const parentData = [{
        Task_ID: 1,
        Task_Parent_ID: 0,
        Task_Subject: "Parent 1"
    }, {
        Task_ID: 2,
        Task_Parent_ID: 0,
        Task_Subject: "Parent 2"
    }];
    const childData = [{
        Task_ID: 10,
        Task_Parent_ID: 1,
        Task_Subject: "Child 1"
    }, {
        Task_ID: 20,
        Task_Parent_ID: 2,
        Task_Subject: "Child 2"
    }];
    const OPERATION_ID_FIRST = 1,
        OPERATION_ID_SECOND = 2;
    beforeEach(() => {
        loadCalls = [];
        mockStore = new CustomStore({
            key: "Task_ID",
            load: options => {
                const deferred = new Deferred;
                loadCalls.push({
                    filter: null === options || void 0 === options ? void 0 : options.filter,
                    deferred: deferred
                });
                return deferred.promise()
            }
        });
        const dataSource = new DataSource({
            store: mockStore,
            reshapeOnPush: true
        });
        const mockComponent = {
            option: jest.fn(key => ({
                remoteOperations: {
                    filtering: true,
                    sorting: true
                },
                parentIdExpr: "Task_Parent_ID",
                hasItemsExpr: "Has_Items",
                filterMode: "fullBranch",
                expandedRowKeys: [],
                dataStructure: "plain",
                rootValue: 0
            } [key])),
            _createActionByOption: jest.fn(() => jest.fn()),
            on: jest.fn(() => mockComponent),
            off: jest.fn(() => mockComponent),
            _eventsStrategy: {
                on: jest.fn(),
                off: jest.fn(),
                fireEvent: jest.fn(),
                hasEvent: jest.fn(() => false)
            }
        };
        dataSourceAdapter = new DataSourceAdapterTreeList(mockComponent);
        dataSourceAdapter.init(dataSource, {
            remoteOperations: {
                filtering: true
            }
        });
        dataSourceAdapter._loadDataSource = jest.fn(options => {
            const deferred = new Deferred;
            loadCalls.push({
                filter: null === options || void 0 === options ? void 0 : options.filter,
                deferred: deferred,
                type: "dataSource"
            });
            return deferred.promise()
        })
    });
    afterEach(() => {
        jest.clearAllMocks();
        jest.restoreAllMocks();
        loadCalls = [];
        dataSourceAdapter._loadDataSource = void 0;
        dataSourceAdapter.loadFromStore = void 0;
        mockStore = void 0;
        dataSourceAdapter = void 0
    });
    test("T1311885 - _loadParentsOrChildren should NOT throw concat error when _cachedStoreData is cleared", async () => {
        let firstLoadDeferred = null;
        let errorMessage = "";
        const unhandledRejectionHandler = reason => {
            errorMessage = (null === reason || void 0 === reason ? void 0 : reason.message) || String(reason)
        };
        process.on("unhandledRejection", unhandledRejectionHandler);
        dataSourceAdapter._cachedStoreData = parentData;
        dataSourceAdapter._dataSource = {
            store: jest.fn(() => mockStore),
            cancel: jest.fn()
        };
        dataSourceAdapter._lastOperationId = OPERATION_ID_FIRST;
        const options = {
            remoteOperations: {
                filtering: true
            },
            storeLoadOptions: {
                sort: null
            },
            loadOptions: {
                sort: null
            },
            operationId: OPERATION_ID_FIRST
        };
        dataSourceAdapter.loadFromStore = jest.fn((loadOptions, store) => {
            const deferred = new Deferred;
            if (!firstLoadDeferred) {
                firstLoadDeferred = deferred
            }
            return deferred.promise()
        });
        dataSourceAdapter._loadParentsOrChildren(childData, options);
        expect(dataSourceAdapter.loadFromStore).toHaveBeenCalledTimes(1);
        expect(firstLoadDeferred).toBeDefined();
        dataSourceAdapter._cachedStoreData = void 0;
        dataSourceAdapter._lastOperationId = OPERATION_ID_SECOND;
        firstLoadDeferred.resolve(parentData);
        await Promise.resolve();
        process.off("unhandledRejection", unhandledRejectionHandler);
        expect(errorMessage).toBe("");
        expect(errorMessage).not.toMatch(/concat|Cannot read properties of undefined/i)
    })
});
