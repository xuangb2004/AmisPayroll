/**
 * DevExtreme (esm/__internal/grids/new/card_view/content_view/content/card/cover.test.js)
 * Version: 25.2.7
 * Build date: Tue May 05 2026
 *
 * Copyright (c) 2012 - 2026 Developer Express Inc. ALL RIGHTS RESERVED
 * Read about DevExtreme licensing here: https://js.devexpress.com/Licensing/
 */
import {
    createComponentVNode,
    normalizeProps
} from "inferno";
import {
    describe,
    expect,
    it
} from "@jest/globals";
import {
    render
} from "inferno";
import {
    Cover
} from "./cover";
describe("Cover", () => {
    it("should render the image correctly", () => {
        const container = document.createElement("div");
        const props = {
            imageSrc: "https://www.devexpress.com/support/demos/i/demo-thumbs/aspnetcore-grid.png",
            alt: "Card Cover",
            className: "cover-image"
        };
        render(normalizeProps(createComponentVNode(2, Cover, Object.assign({
            card: {}
        }, props))), container);
        const image = container.querySelector("img");
        expect(image).not.toBeNull();
        expect(null === image || void 0 === image ? void 0 : image.src).toBe(props.imageSrc);
        expect(null === image || void 0 === image ? void 0 : image.alt).toBe(props.alt);
        expect(null === image || void 0 === image ? void 0 : image.className).toContain(props.className)
    });
    describe("when there is no image", () => {
        it("should render image thumbnail", () => {
            const container = document.createElement("div");
            render(normalizeProps(createComponentVNode(2, Cover, Object.assign({
                card: {}
            }, {}))), container);
            expect(container).toMatchSnapshot()
        })
    })
});
