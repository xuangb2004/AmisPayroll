/**
 * DevExtreme (cjs/__internal/core/license/lcp_key_validation/lcp_key_validation.test.js)
 * Version: 25.2.7
 * Build date: Tue May 05 2026
 *
 * Copyright (c) 2012 - 2026 Developer Express Inc. ALL RIGHTS RESERVED
 * Read about DevExtreme licensing here: https://js.devexpress.com/Licensing/
 */
"use strict";
var _globals = require("@jest/globals");
var _version = require("../../../../core/version");
var _version2 = require("../../../utils/version");
var _types = require("../types");
var _lcp_key_validator = require("./lcp_key_validator");
var _license_info = require("./license_info");
var _product_info = require("./product_info");
const DOT_NET_TICKS_EPOCH_OFFSET = 621355968000000000n;
const DOT_NET_TICKS_PER_MS = 10000n;
const DEVEXTREME_HTML_JS_BIT = 1n << 54n;

function msToDotNetTicks(ms) {
    return (10000n * BigInt(ms) + 621355968000000000n).toString()
}

function createLcpSource(payload) {
    const signature = "A".repeat(136);
    return `LCPv1${btoa(`${signature}${payload}`)}`
}

function loadParserWithBypassedSignatureCheck() {
    _globals.jest.resetModules();
    _globals.jest.doMock("./utils", () => {
        const actual = _globals.jest.requireActual("./utils");
        return Object.assign({}, actual, {
            encodeString: text => text,
            shiftDecodeText: text => text,
            verifyHash: () => true
        })
    });
    const {
        parseDevExpressProductKey: parseDevExpressProductKey
    } = require("./lcp_key_validator");
    const {
        TokenKind: TokenKind
    } = require("../types");
    return {
        parseDevExpressProductKey: parseDevExpressProductKey,
        TokenKind: TokenKind
    }
}

function getTrialLicense() {
    const {
        major: major,
        minor: minor
    } = (0, _version2.parseVersion)(_version.version);
    const products = [(0, _product_info.createProductInfo)(parseInt(`${major}${minor}`, 10), 0n)];
    return {
        products: products
    }
}(0, _globals.describe)("LCP key validation", () => {
    (0, _globals.it)("serializer returns an invalid license for malformed input", () => {
        const token = (0, _lcp_key_validator.parseDevExpressProductKey)("not-a-real-license");
        (0, _globals.expect)(token.kind).toBe(_types.TokenKind.corrupted)
    });
    (process.env.DX_PRODUCT_KEY ? _globals.it : _globals.it.skip)("developer product license fixtures parse into valid LicenseInfo instances", () => {
        const token = (0, _lcp_key_validator.parseDevExpressProductKey)(process.env.DX_PRODUCT_KEY);
        (0, _globals.expect)(token.kind).toBe(_types.TokenKind.verified)
    });
    (0, _globals.it)("trial fallback does not grant product access", () => {
        const trialLicense = getTrialLicense();
        (0, _globals.expect)((0, _license_info.isLicenseValid)(trialLicense)).toBe(true);
        const version = (0, _license_info.findLatestDevExtremeVersion)(trialLicense);
        (0, _globals.expect)(version).toBe(void 0)
    });
    (0, _globals.it)("does not classify a valid DevExtreme product key as trial-expired when expiration metadata is in the past", () => {
        const {
            parseDevExpressProductKey: parseDevExpressProductKey,
            TokenKind: TokenKind
        } = loadParserWithBypassedSignatureCheck();
        const expiredAt = msToDotNetTicks(Date.UTC(2020, 0, 1));
        const payload = `meta;251,18014398509481984,0,${expiredAt};`;
        const token = parseDevExpressProductKey(createLcpSource(payload));
        (0, _globals.expect)(token.kind).toBe(TokenKind.verified)
    });
    (0, _globals.it)("returns trial-expired for expired trial keys without DevExtreme product access", () => {
        const {
            parseDevExpressProductKey: parseDevExpressProductKey,
            TokenKind: TokenKind
        } = loadParserWithBypassedSignatureCheck();
        const expiredAt = msToDotNetTicks(Date.UTC(2020, 0, 1));
        const payload = `meta;251,0,0,${expiredAt};`;
        const token = parseDevExpressProductKey(createLcpSource(payload));
        (0, _globals.expect)(token.kind).toBe(TokenKind.corrupted);
        if (token.kind === TokenKind.corrupted) {
            (0, _globals.expect)(token.error).toBe("trial-expired")
        }
    })
});
