/**
 * DevExtreme (cjs/__internal/ui/text_box/utils.scroll.js)
 * Version: 25.2.7
 * Build date: Tue May 05 2026
 *
 * Copyright (c) 2012 - 2026 Developer Express Inc. ALL RIGHTS RESERVED
 * Read about DevExtreme licensing here: https://js.devexpress.com/Licensing/
 */
"use strict";
Object.defineProperty(exports, "__esModule", {
    value: true
});
exports.prepareScrollData = exports.allowScroll = void 0;
var _index = require("../../../common/core/events/utils/index");
var _renderer = _interopRequireDefault(require("../../../core/renderer"));

function _interopRequireDefault(e) {
    return e && e.__esModule ? e : {
        default: e
    }
}
const allowScroll = (container, delta, shiftKey) => {
    const $container = (0, _renderer.default)(container);
    const scrollTopPos = parseFloat(shiftKey ? $container.scrollLeft() : $container.scrollTop());
    const prop = shiftKey ? "Width" : "Height";
    const scrollSize = parseFloat($container.prop(`scroll${prop}`));
    const clientSize = parseFloat($container.prop(`client${prop}`));
    const scrollBottomPos = Math.trunc(scrollSize - clientSize - scrollTopPos);
    if (0 === scrollTopPos && 0 === scrollBottomPos) {
        return false
    }
    const isScrollFromTop = 0 === scrollTopPos && delta >= 0;
    const isScrollFromBottom = 0 === scrollBottomPos && delta <= 0;
    const isScrollFromMiddle = scrollTopPos > 0 && scrollBottomPos > 0;
    if (isScrollFromTop || isScrollFromBottom || isScrollFromMiddle) {
        return true
    }
    return
};
exports.allowScroll = allowScroll;
const prepareScrollData = (container, validateTarget) => {
    const $container = (0, _renderer.default)(container);
    const scrollData = {
        validate: e => {
            if ((0, _index.isDxMouseWheelEvent)(e) && (eventTarget = e.target, validateTarget ? (0, _renderer.default)(eventTarget).is(container) : true)) {
                if (allowScroll($container, -(e.delta ?? 0), e.shiftKey)) {
                    e._needSkipEvent = true;
                    return true
                }
                return false
            }
            var eventTarget;
            return false
        }
    };
    return scrollData
};
exports.prepareScrollData = prepareScrollData;
